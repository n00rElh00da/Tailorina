import {useState} from "react";

function Logo () {

    return(
        <>
        
        
        </>
    );
}

export default Logo;

/*
export function GarmentCard ({garment}) {
    
     const [price , setPrice] = useState(garment.price) ; 
    return (
       <>
         <p>price: {price}</p>

         <button onClick={() => setPrice(price + 100)}></button>
       </>
    );
}*/

